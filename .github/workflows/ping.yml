# Diagnóstico temporário: distingue falha de conta/infra (este job também falha
# na hora) de problema no ci.yml (este job roda normalmente).
name: Ping

on:
  workflow_dispatch:

jobs:
  ping-ubuntu:
    name: Ping ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - run: echo "runner ubuntu OK em $(date -u)"
